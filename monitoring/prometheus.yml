global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # Prometheus 자체 모니터링
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node Exporter - 시스템 메트릭
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

  # cAdvisor - 컨테이너 메트릭 (선택사항)
  # - job_name: 'cadvisor'
  #   static_configs:
  #     - targets: ['cadvisor:8080']

  # Spring Boot Admin App (EC2에서는 Docker 컨테이너명 사용)
  - job_name: 'admin-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['admin-app:8080']
    scrape_interval: 60s
    scrape_timeout: 30s

